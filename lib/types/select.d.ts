// This is a declaration file to help with the Select component
// It extends the types from the original @rn-primitives/select component

declare module '@rn-primitives/select' {
    // Make Option compatible with string type for easier use
    export type Option = string;
    
    // Rest of the exported types remain the same
  }